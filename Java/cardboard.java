import java.io.*;
import java.util.Collections;
class cardboard{
    public static void card(int ver[],int hor[]){
       Arrays.sort(ver, Collections.reverseOrder());
       Arrays.sort(hor, Collections.reverseOrder());
       int v=ver.length;
       int h=hor.length;
       int ve=1,ho=1;
       
    }
    public static void main(String[]args){

    }
}